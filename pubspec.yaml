name: restaurant_map_reviews
description: A restaurant review and listing application
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_map: ^5.0.0
  latlong2: ^0.9.0
  geolocator: ^10.1.0
  cloud_firestore: ^4.13.0
  firebase_core: ^2.22.0
  firebase_auth: ^4.14.0
  provider: ^6.1.1
  image_picker: ^1.0.4
  cached_network_image: ^3.3.0
  shared_preferences: ^2.0.11

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true